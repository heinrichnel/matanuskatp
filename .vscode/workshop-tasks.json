{
  "tasks": [
    {
      "id": "sync-workshop-sidebar",
      "label": "🔧 Sync Workshop Sidebar + Components",
      "type": "custom",
      "command": "validateSidebarAndGenerateComponents",
      "description": "Ensures all /workshop sidebar routes link to valid components. Auto-generates missing views using actual business logic. Validates Firestore bindings.",
      "files": [
        "src/components/layout/Sidebar.tsx",
        "src/pages/WorkshopPage.tsx",
        "src/components/workshop/**/*",
        "src/components/tyre/**/*",
        "src/components/ui/**/*",
        "src/firebase.ts",
        "src/types/**/*"
      ],
      "rules": [
        "All workshop routes in Sidebar.tsx must have corresponding route definitions in Layout.tsx or App.tsx.",
        "Each /workshop path must map to a component under src/components/workshop or src/pages.",
        "Missing components must be generated with real data logic based on types in /types/ and Firestore collections.",
        "UI must reuse design system from /components/ui/ (Card, Tabs, Modal, etc).",
        "Firestore bindings must use existing functions from firebase.ts with onSnapshot or getDocs.",
        "No placeholder <div>Coming Soon</div> components allowed. Replace with data-driven views."
      ],
      "output": {
        "missingComponentsReport": "logs/workshop-missing-components.md",
        "fixedRoutesLog": "logs/workshop-route-fixes.md"
      },
      "allowWrite": true,
      "allowRead": true,
      "allowEdit": true
    }
  ]
}
