# ───────── Node Dependencies ───────────────
node_modules/
.pnp/
.pnp.js

# ───────── Production Builds ───────────────
dist/
build/

# ───────── Environment Files (Secrets) ─────
.env*
!.env.example

# ───────── Firebase & GCP Local Artifacts ──
.firebase/
firebase-debug.log*
firestore-debug.log*
functions/lib/
# .firebaserc is typically committed for CI/CD

# ───────── Debug Logs ──────────────────────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
ui-debug.log*

# ───────── System Artifacts ────────────────
.DS_Store
.qodo
.bolt

# ───────── Local Emulator Configuration ────
# Note: Source files should usually be committed
# Remove these if they are part of your codebase:
# firebaseEmulators.ts
# webhookSenders.ts
# schemadashboard1.tsx

# ───────── Coverage & Testing ──────────────
coverage/
*.lcov
.nyc_output

# ───────── TypeScript artifacts ─────────────
*.tsbuildinfo

# ───────── VS Code Remote Dev ──────────────
.vscode/
.history/
*.code-workspace

# ───────── IDEs & Editors ────────────────
.idea/

# ───────── Firebase Hosting & Rules ─────
# These config files are typically committed:
# firebase.json
# database.rules.json
# storage.rules

# ───────── GitHub & Workflows (if private CI) ───────────
.github/
# But keep markdowns:
!.github/**/*.md

# ───────── CSV/Excel/JSON Dumps ─────────────
*.csv
*.xls
*.xlsx
*.json
!src/**/*.json  # keep config files used in code
!package.json
!package-lock.json
!tsconfig*.json
!firebase.json
!firestore*.json
!database*.json
!storage*.json

# ───────── Build Output for Functions ───────
functions/node_modules/
functions/lib/
functions/.tsbuildinfo
public/functions/node_modules/

# ───────── Local Netlify folder ─────────────
.netlify

# ───────── Storybook ─────────────────────
*storybook.log
storybook-static

# ───────── Vercel ──────────────────────────
.vercel
node_modules/express/lib/application.js
